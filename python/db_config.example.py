# Copy this file to db_config.py (which is git-ignored) and fill in your
# own MySQL credentials. db_connection.py will pick these up automatically
# if the DB_* environment variables are not set.

HOST = "localhost"
USER = "root"
PASSWORD = "your_password_here"
DATABASE = "online_book_store"
